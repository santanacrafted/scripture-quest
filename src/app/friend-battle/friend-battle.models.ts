import { Timestamp } from 'firebase/firestore';
export interface BattleInvitation { id:string; challengerId:string; recipientId:string; participantIds:string[]; type:'friend-battle'; status:'pending'|'accepted'|'declined'|'cancelled'|'expired'; rounds:number; createdAt:Timestamp; updatedAt:Timestamp; expiresAt:Timestamp; matchId:string|null; challenger?:{displayName:string;username?:string;avatarUrl:string|null}; recipient?:{displayName:string;username?:string;avatarUrl:string|null}; }
export interface FriendBattleMatch { id:string; mode:'friend-battle'|'quick-match'; status:'lobby'|'waiting'|'active'|'completed'|'cancelled'; createdAt?:Timestamp; updatedAt?:Timestamp; playerIds:string[]; hostPlayerId?:string; invitationId?:string; totalRounds:number; roundNumber:number; currentTurnPlayerId:string|null; players:Record<string,{displayName:string;username?:string;avatarUrl:string|null;ready:boolean;status:string;score:number}>; }
